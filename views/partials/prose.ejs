<style>
    /* Typography for rendered Markdown. Shared by the post page and the editor
       preview so what you write is what you get. */
    .blog-content>*:first-child {
        margin-top: 0;
    }

    .blog-content h1,
    .blog-content h2,
    .blog-content h3,
    .blog-content h4 {
        font-weight: 700;
        line-height: 1.25;
        margin-top: 2rem;
        margin-bottom: .75rem;
    }

    .blog-content h1 {
        font-size: 1.9rem;
    }

    .blog-content h2 {
        font-size: 1.5rem;
    }

    .blog-content h3 {
        font-size: 1.25rem;
    }

    .blog-content h4 {
        font-size: 1.1rem;
    }

    .blog-content p,
    .blog-content ul,
    .blog-content ol {
        margin-bottom: 1.15rem;
    }

    .blog-content li {
        margin-bottom: .35rem;
    }

    .blog-content blockquote {
        border-left: 3px solid #d1d5db;
        padding-left: 1rem;
        margin: 1.5rem 0;
        color: #4b5563;
        font-style: italic;
    }

    .blog-content code {
        background: #f3f4f6;
        border-radius: .25rem;
        padding: .1rem .35rem;
        font-size: .9em;
        color: #b91c1c;
    }

    .blog-content pre {
        background: #f3f4f6;
        border-radius: .5rem;
        padding: 1rem;
        overflow-x: auto;
        margin-bottom: 1.15rem;
    }

    .blog-content pre code {
        background: none;
        padding: 0;
        color: inherit;
    }

    .blog-content img {
        max-width: 100%;
        border-radius: .5rem;
    }

    .blog-content hr {
        margin: 2rem 0;
    }

    /* Wide tables scroll inside the article instead of widening the page. */
    .blog-content table {
        width: 100%;
        margin-bottom: 1.15rem;
        border-collapse: collapse;
        display: block;
        overflow-x: auto;
    }

    .blog-content th,
    .blog-content td {
        border: 1px solid #e5e7eb;
        padding: .5rem .75rem;
        text-align: left;
    }

    .blog-content th {
        background: #f9fafb;
    }
</style>
