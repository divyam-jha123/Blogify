## strict command for commiting and pushing the code

1. use ga . for staging all the files
2. use git commit -m "commit message" for commiting the code
3. use git push origin <branch-name> for pushing the code

`dont use signed meesge by claude code at the end of the commit message`